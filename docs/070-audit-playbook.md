# 070 · 审计方法

版本：1.0.0 ｜ 日期：2026-08-25 ｜ 依据：第一轮审计（design-ui 3.2 → 6.0）

第一轮的结论写在 Artifact 的「审计报告」里。**这一份写的是怎么做那一轮，以及
那一轮的方法在哪里失灵。**

## 1. 六条不要相信

第一轮踩到的，按代价排：

### 1.1 不要把「绿」当成「查过」

`pnpm guardrails` 打印 `Design System guardrails passed` 的同时，`check-design-system`
里有**四条检查是死的**——6 处字面量退格符让它们的正则永远匹配不上。

发现它靠的是 SonarCloud 报了个「控制字符」：**另一个工具、另一条完全无关的理由**。
换句话说是撞上的，不是查出来的。

> **守卫坏掉的表现，和它正常工作的表现，都是「绿」。**

对策：`pnpm test:guardrails`（变异测试）。审计**第一步**就跑它，在读任何守卫输出
之前——因为后面所有结论都建立在守卫说真话的前提上。

### 1.2 不要转述工具的结论

第一轮把一条 Sonar CRITICAL 原样写进了报告，理由还写反了（见
`docs/audit/known-false-positives.md`）。

工具给的是**信号**，不是结论。每条要写进报告的发现，都必须**自己看过那段代码**并
能说清：它错在哪、照它改会发生什么。说不清就不写。

### 1.3 不要把结构变成描述再从描述重建

三份文档的 CSS 是逐字节抽出来的，一字不差；导轨是照着理解重打的，组顺序整个反了。

> **顺序活不过摘要。**

对策：能复制就不要复述。要比对就写脚本比对（`check-doc-shape`），不要靠眼睛。

### 1.4 消费端报来的「根因在 DS」，先做对照组

2026-08-26 opera 报来一条：React 的 key 警告指着他们的 `page.tsx:231`，追进 DS 的
构建产物，看到 `Section` 用的是 `jsx` 而不是 `jsxs`，结论是「根因在 DS，改一个字符
就行」，并登记了技术债 TD-011。

链条讲得通，但结论是错的。三个对照组一跑就清楚（用例见当时的临时探针，结论已固化
成 `tests/setup.ts` 里的 key 守卫）：

| 对照组                                             | 结果         |
| -------------------------------------------------- | ------------ |
| `<Section>{a}{b}{c}</Section>` 静态多孩子          | **不报警**   |
| 中间层拼数组 → 交给 `Section`                      | 报警         |
| 中间层拼数组 → 交给**裸 `<div>`**（DS 完全不参与） | **同样报警** |
| 中间层改用带 key 的 Fragment → 交给 `Section`      | 不报警       |

裸 `<div>` 报的是同一条，所以跟 DS 无关。而第一行才是关键：**`Section` 内部对
children 用 `jsx` 并不会引发警告**——静态多孩子在外层 `jsxs(Section, …)` 那一步就
被标记成已校验了，内层再迭代一次不会重复报。

顺带一提，那个「一行修复」并不存在：源码里写的是 `{children}`，`jsx` / `jsxs` 由编译器
按调用点的字面孩子决定，没有哪一行可以改成 `jsxs`。

**为什么会查到 Section 头上**——因为 React 的警告原文点了两个名字，恰好都不是真凶：

```
Check the render method of `Section`.          ← 收下这个数组的件（DS 的）
It was passed a child from AccountDetailPage.  ← 元素被创建的地方（页面）
```

真正拼出无 key 数组的那一层（他们的 `DecisionChain`，DS 全仓没有这个名字）**一个字
都没被提到**。查的人顺着两个名字走，一个通向 DS，一个通向页面——于是绕开了中间层。

> **消费端说「根因在你这」时，先造一个不含 DS 的对照组。** 症状在对照组里照样出现，
> 讨论就结束了；照样不出现，才轮到看 DS 的实现。

最后一点判断：这条警告**不能压**。`[a, b].filter(Boolean)` 少一个成员时后面的会落到
前面的索引上，React 按索引复用实例——同类型的兄弟之间状态会串（实测点到 `B:3` 的
计数器在 A 消失后变回 `B:0`，它捡了 A 的那一格）。DS 若用 `Children.toArray` 之类
的手法把警告吃掉，就是把消费端的真缺陷盖住。DS 该做的是保证自己不出这个形状——
已落成 `tests/setup.ts` 里的 key 守卫，整套 189 条用例目前零警告。

### 1.5 不要接受一个太顺理成章的解释

2026-08-26 GitHub Actions 出了一次 critical 级故障，我们一个 PR 的 CI run 卡在 `queued`。状态页说「积压正在消化，一小时内完成」——**这个解释太合身了**，于是我照着它等，一等 142 分钟。

真正的判据一直摆在那儿，一条 API 就查得到：

```
status=queued   updated_at = created_at = 15:11:31Z   ← 142 分钟没被碰过
```

**孤儿 run 与排队中的 run 长得一模一样，都是 `queued`。** 区分它们的是 `updated_at`：真在排队的 run 会被反复更新（分片调度、优先级重算都会写它），孤儿的 `updated_at` 永远等于 `created_at`。

更能说明问题的是 GitHub 自己前后矛盾：

```
gh run cancel  →  "Cannot cancel a workflow run that is completed"
gh api  run    →  status=queued
```

取消接口说它已完成，查询接口说它在排队——**两边对不上就是状态坏了**，那一刻就该动手，而不是继续等。

`gh pr close` + `gh pr reopen` 重新触发 `pull_request` 事件（默认 activity types 含 `reopened`），**20 秒后 in_progress**，2 分 9 秒跑完。队列对我们一直是通的。

> **一个平台故障正在进行时，「还没轮到我」是个太容易接受的解释。**
> 它解释得了现象，但它没有被验证过——而验证它只要一个字段。
>
> 这与 §1.1 是同一条：**「说得通」不等于「查过了」。**

顺带一提：拆开这个僵局的不是我的排查，是消费侧一句「其他任务线都好了」。**外部对照组**——同 §1.4 是一条判据。

### 1.6 也不要把红当成「查出问题了」

本机跑 `pnpm format:check` 恒红，56 个文件不合格式，其中大半是从没碰过的。

查下来**唯一差异是行尾**：`core.autocrlf=true` 让 Windows 磁盘上是 CRLF，而 prettier 默认 `endOfLine: "lf"`。把同一个文件转成 LF 再检就通过。CI 跑在 Linux 上、文件是 LF，所以历来都过。

> **本机的 `pnpm format:check` 不是信号。**
> 复现 CI 的其余每一步都有效：`build` / `type-check` / `lint` / `guardrails` / `test:guardrails` / 测试 / `build:preview`。只有这一条要跳过。

§1.1 说的是不要把绿当成查过；这一条是它的反面——**一个恒红的检查同样什么都没查**，而且更费时间：它每次都要求你重新说服自己「这次也是行尾问题」。

## 2. 顺序

每一步都不可跳过，且**顺序有意义**：先证明仪器可信，再用仪器。

| #   | 做什么           | 命令                                         | 为什么在这个位置                             |
| --- | ---------------- | -------------------------------------------- | -------------------------------------------- |
| 1   | 工作树必须干净   | `git status --porcelain`                     | 后面几步会改真实文件再还原                   |
| 2   | **守卫自测**     | `pnpm test:guardrails`                       | 先证明仪器活着。这一步红，后面全部无效       |
| 3   | 基线快照         | `node scripts/audit/snapshot.mjs`            | 与上一轮 diff。测法与数字一起入仓，所以可比  |
| 4   | 跑全部守卫       | `pnpm guardrails`                            | 此刻它的「绿」才有意义                       |
| 5   | 逐包验证         | `pnpm type-check && pnpm lint && pnpm build` | 按依赖方向                                   |
| 5.5 | **组件行为测试** | `pnpm test`                                  | 自测证明仪器活着，这一条测件本身跑起来对不对 |
| 6   | 外部工具         | SonarCloud API                               | 拿信号，不拿结论                             |
| 7   | 对照误报登记     | `docs/audit/known-false-positives.md`        | 别重新讨论已经判过的                         |
| 8   | 逐条核实         | ——                                           | 每条发现自己看代码，写清失败场景             |
| 9   | 写报告           | `docs/artifacts/audit.html`                  | 底本入仓，形制由 `check-doc-shape` 守        |
| 10  | 写入基线         | `node scripts/audit/snapshot.mjs --write`    | 给下一轮留下可比的起点                       |

## 3. 分类判据

按**缺陷是怎么逃过检查的**分，不按它在哪个文件——因为那才决定下一条同类缺陷会不会
再逃一次。第一轮分出四类，够用：

| 类                   | 特征                   | 典型                                 |
| -------------------- | ---------------------- | ------------------------------------ |
| 静默失败             | 构建全绿，只是结果不对 | 类名哑火、挡位用错、body 锁死        |
| 契约只写在散文里     | 判据成文但没有载体     | `danger` 只染色不设防、文案出口      |
| **守卫自己停止守卫** | 表现是「绿」           | 退格符死正则、扫描面被白名单缩小     |
| 分层与打包的隐性约束 | 约束只写在运行时行为里 | server 入口崩、引用环、`Omit` 不分配 |

新发现如果四类都装不下，先怀疑是分类不够，再考虑加一类——**类别的价值在于它能预测
下一条**，不在于把清单填满。

## 3.1 写行为测试的三条判据

搭基座时立刻踩到一次，值得写下来。

### 通过两次的测试等于没测

第一版「整页锁死回归」断言的是「关掉确认框后 body 干净」。它绿了——**把
`modal={false}` 改回 `modal` 之后它还是绿的**。实测确认 jsdom 重现不了那个残留：
真实故障依赖两个锁的竞争时序，而 jsdom 的同步事件派发把竞争抹平了。

> **每写一条回归测试，都要把缺陷放回去跑一次。** 不红，这条测试就是装饰。

改法：换一个能稳定分辨两版的断言。模态菜单一开就给 body 挂 `data-scroll-locked`，
非模态的不挂——于是断言「菜单单独开着时不上锁」。它钉的是**机制**（不上锁就没有
竞争），不是症状，注释里写明了这个区别。

### 变异不红，先查代码，别急着加断言

2026-08-26 铺 hooks 用例时撞到第二次，比上一条更值钱。

把 `exitFullscreen` 里的 `unlockScroll()` 删掉，「退出时还回滚动」那条**照样绿**。
第一反应是断言太松，想再加两句。往下追才发现不是测试的问题：那个标着
**「组件卸载时清理」** 的 effect，依赖数组里带着 `state.isFullscreen`——effect 的清理
**每次依赖变化都会跑**，不只是卸载。于是退出的那一帧它先跑了一遍清理，把
`exitFullscreen` 自己那次 unlockScroll 顶掉了。

顺带两个后果：原生模式下退出会**调两次** `exitNativeFullscreen()`；注释和代码说的
不是一回事，而注释是唯一能看出作者本意的东西。

> **变异测试留绿，有两种解释：测试没钉住，或者代码里有条多余的路在兜着。**
> 先分清是哪一种。直接补断言，等于把那条多余的路一起焊死。

改法是把清理 effect 做成真正的卸载时（空依赖 + 一个 ref 拿最新状态），`exitFullscreen`
里那次调用重新变成唯一的退出路径——这时两条变异（退出不还原、卸载不还原）才**各自
都能变红**，说明两条路是真的分开了。

### 测试不许逼守卫开口子

测试放在 `packages/design-ui/tests/` 而不是 `src/**/__tests__/`，因为 `src/` 下有两条
守卫在扫：`check-i18n-seam` 禁中日韩字符（而用例名是写给维护者的中文散文），
`check-preview-coverage` 要求 `components/` 下每个 `.tsx` 都有预览条目。

**为了迁就测试而给守卫开豁免，是把安全网剪个洞。** 挪个目录就没有这个问题。

## 4. 报告的硬要求

- **每条发现给出失败场景**：什么输入 → 什么错误结果。给不出就还没查清
- **写明是谁抓到的**：机器抓的写哪条守卫，人抓的写谁提的。第一轮两条最贵的发现
  都是消费侧评审提的，这件事必须写进报告，否则下一轮会高估机器
- **不写没验证的数字**。数字要带测法（见基线快照的 `how` 字段）
- **勘误留在原地**，不要把错的那段悄悄改掉——读者需要知道结论变过

## 5. 下一轮的已知缺口

**口径已经换过一次，先说为什么。** 前四轮铺用例是按「件名」排的——先出过事的件，再同族的件，最后模板。这个口径现在衰减了：全仓还有 410 个分支没跑过，其中 **240 条（59%）落在已经有用例的文件里**。按件名排会以为它们「测过了」。

同时推翻一条我自己说过、没验证的话：剩下的零覆盖文件「多半是薄封装」。**按行数成立，按分支不成立**——`MetricGrid` 4 行 24 分支，`PanelCard` 1 行 9 分支。而前四轮修掉的缺陷（Drawer 挡位失效、`MetricListCard` 与 `EntryCard` 的键盘陷阱、`FullscreenProvider` 的清理时机）**全是分支缺陷**。行数是错的风险代理。

### 5.1 下一轮的清单

#### 先纠一个数字：此前报的覆盖率只覆盖三个包里的一个

`snapshot.mjs` 从接入那天起只读 `packages/design-ui/coverage/…`，而 `how` 字段——它存在的全部意义就是「下一轮照 how 复测才可比」——**漏了这个限定词**。于是每一份报告都少说了一句：「83.9%」是 design-ui 的。

漏掉的那个包正是 **design-system**，而**应用只装伞包**。这与 hooks 那次是同一个形状：它不在按包排的清单里，因为清单是按「有没有 test 脚本」隐式排的。

|            | design-ui | design-system | **全仓（发布出去的代码）** |
| ---------- | --------- | ------------- | -------------------------- |
| 行覆盖     | 84.2%     | **98.6%**     | **87.6%**                  |
| 分支覆盖   | 86.0%     | **94.1%**     | **88.7%**                  |
| 未覆盖分支 | 127 / 907 | **26 / 442**  | **153 / 1349**             |

（2026-08-27 收口后的数。此前是 design-ui 83.9% / design-system 64.9% 与 42.1%、
全仓 79.5% / 71.4%，未覆盖分支 386 条。）

指标名与 `how` 都已带上包名，并新增「全仓」两行——**唯一诚实的总数**。

#### 伞包已铺的与未铺的

2026-08-26/27 补齐测试基座（vitest 配置 / setup / `tests` 进 type-check），已铺三批：

| 文件                                                         | 状态       | 未覆盖分支 |
| ------------------------------------------------------------ | ---------- | ---------- |
| `theme/script.ts` · `density/densityConfig.ts`               | ✅ 100%    | 0          |
| `theme/ThemeProvider.tsx`                                    | ✅ 行 100% | 5 / 28     |
| `theme/fontSizePreference.ts`                                | ✅ 行 100% | 5 / 29     |
| `components/shell/ShellSearchBox.tsx`                        | ✅ 行 86%  | 6 / 44     |
| `components/shell/ShellBootScreen.tsx` · `ShellLauncher.tsx` | ✅ 100%    | 0          |
| `components/shell/ShellChrome.tsx`                           | ✅ 行 100% | 6 / 162    |
| `components/shell/ShellPanel.tsx`                            | ✅ 行 100% | 3 / 96     |
| `components/shell/ShellSidebarNav.tsx`                       | ✅ 100%    | 0 / 63     |

**伞包已铺完**（2026-08-27）。第六批打掉最后的 240 条：`ShellPanel` 96 + `ShellSidebarNav` 63 + `ShellChrome` 剩余 81（集中在 `ShellLocaleSwitcher` / `ShellPreferencePanel` / `ShellUserMenu` 三个 Popover 件），新增用例 149 条、变异用例 99 条全部验红。剩下的 26 条分散在 `ShellSearchBox`(6) / `ThemeProvider`(5) / `fontSizePreference`(5) / `ShellChrome`(6) / `ShellPanel`(3) / `boot-splash`(1)，都是薄分支，**建议与 design-ui 的尾部同样处理：不追**。

#### design-ui 剩余（建议不追）

129 条未覆盖分支，头一名 `MetricListCard` 10 条，其余 5~7 条。四个真缺陷全是**变异留绿追出来的，不是覆盖率追出来的**；尾部只剩薄封装的 className 分支。

### 5.1.1 第四批学到的：**「不给就不渲染那一层」要数元素，不要数文字**

结构族第一轮变异有 5 条留绿，**全是同一个形状**：断言写的是「文字不在」或「角色不在」，而把 `{action ? … : null}` 改成恒渲染之后，渲染出来的是一个**空壳** —— 既没有文字也没有角色，从内容上分辨不出。

而空壳有代价：它带着 `gap` 与 `self-end`，会在标题块旁留出一段说不清来路的空白。

> **这类分支一律数元素**（`root.children` 的长度），不数文字。

同一轮还撞到第二个形状：`aria-hidden` 的断言写成「页面里存在某个 `aria-hidden`」，**被内层 svg 自带的属性满足**，把外层那一层拿掉也不变红。断言要落在**那一层本身**上。

### 5.1.2 第五批学到的：**测试代码此前从来没被类型检查过**

`packages/design-ui/tsconfig.json` 的 `include` 只有 `src`。接上 `tests` 之后一次报出 11 条真错误，其中 5 条是**无效的图标名**（`gear` 不在词表里）——写在源码里会编译失败，写在用例里一路绿到底。

代价不是假绿：用例照样跑、照样通过。代价是**用例可以断言一个类型上根本不成立的用法**——那条断言测的是一个不存在的世界。

`tests` 现已纳入 `pnpm type-check`。

### 5.1.3 第五批学到的：`toContain` 是子串匹配

断言 className 时用 `expect(root.className).toContain("has-[…]:border-destructive")`——**删掉那条类之后照样绿**，因为 `dark:has-[…]:border-destructive/50` 仍然含这个子串。Tailwind 的变体天然让一条类成为另一条的子串。

> 断言 className **按 token 比**（`className.split(" ")`），不要 `toContain`。

### 5.1.4 一条没人验的理由，和一条从没生效过的守卫是同一类东西

`self-test.mjs` 里曾有一张「未做变异测试的守卫」表，三条各附一条理由。这张表在那里躺了一整轮，谁也没去验。**三条理由查下来两条不准、一条完全错：**

| 守卫                         | 表上写的                                                   | 实际                                                                                                             |
| ---------------------------- | ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `check-mode-blocks`          | 「任何单点变异都会同时改动两侧，造不出『只坏一边』的输入」 | **完全错**。删掉暗色块里一个变量声明就是只坏一边，守卫当场报「键集与默认块不一致——缺 1（background）」           |
| `check-server-entry-safety`  | 「变异源码后必须重新 build 才生效」                        | **不必**。它读的就是 `dist`，直接变异 `dist` 即可，17 秒                                                         |
| `check-packed-consumability` | 「同上，读的是 dist 与 pnpm pack 的产物」                  | 贵在 `npm pack` 不在 build；而且它还读**随包发出的 CSS 源文件**——改 `globals.css` 的 `@source` 就够，不用碰 dist |

三条的变异都已补上，**10 条守卫现在全部有变异用例**，没有跳过项。

> 这张表长得像已经想过了 —— 那正是它的危险处。**一条写下来没人验的理由，和一条从没生效过的守卫，坏的方式完全一样：都在「绿」里躲着。**

补的过程里还自己犯了一次同样的错：`check-packed-consumability` 第一次实测 140 秒，据此给它开了一条「慢车道」（默认跳过、只在 CI 跑）。量准之后拆掉了——140 秒是冷启动，稳定态 27~41 秒，全套自测带上它 66 秒、不带 48 秒。**差 18 秒撑不起一条会让人漏跑的分支。**

### 5.1.5 一段没有任何静态检查、失败时静默的代码，跑在所有页面的最前面

`themeBootstrapScript` 是**字符串形态的 JavaScript**：

- 注进每个门户每一页的 `<head>`，在 React 接管之前同步执行
- 是字符串，所以 **tsc 不看它、eslint 不看它**——两道静态关卡在它面前都是瞎的
- 自带 `try { … } catch (_) {}`，**把一切吞掉**

三条合起来：写错了不会有任何声音。表现不是报错，是「主题就是不生效」——而它存在的唯一理由（防首帧白闪）失败时同样不报错，只是闪一下。

**它一直不在任何清单上，因为清单按「件」和「包内测试」排，字符串两头都不占。**

已补 20 条用例（在 jsdom 里 `eval` 这段字符串，喂不同的 localStorage / matchMedia 组合），9 条变异全红——包括「缺省档写成 light」（新用户 + 深色系统那一格，正是白闪最刺眼的一格）与「catch 去掉」（隐私模式下整页脚本断掉）。

顺带查到 `classList.toggle('dark', …)` 里的 `'dark'` 是写死的字面量，而存储键与缺省档都走常量。已改走 `DARK_CLASS`——**运行时求值逐字节相同**，所以不涉及发版。

### 5.2 其余缺口

| 缺口                     | 现状                                              | 补法                                                                                                                                                                                                                                                                                                                                       |
| ------------------------ | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ~~三条守卫没有变异测试~~ | **已补齐**（2026-08-26）：10 条守卫全部有变异用例 | **三条理由查下来两条不准、一条完全错**——见 §5.1.4                                                                                                                                                                                                                                                                                          |
| ~~两条 vulnerability~~   | **已修**（2026-08-26）                            | `--ignore-scripts` 加进 CI 的 action，并在 `pnpm-workspace.yaml` 补 `onlyBuiltDependencies: []`——两道，前者管 CI、后者管所有安装且可清点；`Toast` 的 `Math.random` 换单调计数器而非 `crypto.randomUUID`（后者安全上下文限定）。**初次处置判定不加 `--ignore-scripts`，理由把成本说重了**，完整经过见 `docs/audit/known-false-positives.md` |
| 消费方看不见             | DS 仓看不到 portals 的调用点与 lockfile           | 由消费仓自己登记；DS 只保证「怎么迁」写清楚。**判据见 §1.4**：他们说根因在 DS 时，先做对照组                                                                                                                                                                                                                                               |
| Artifact 文档集          | 六份齐了（2026-08-26 补完接口文档与设计语言）     | 见 `000-doc-map.md` §3                                                                                                                                                                                                                                                                                                                     |

### 5.1.6 第六批学到的：**两个真缺陷都是「邻居已经做了这件事」**

shell 族第二批（`ShellPanel` 96 + `ShellSidebarNav` 63 + `ShellChrome` 剩余 81）写出来两个真缺陷，形状**完全一样**——都是「这件事已经有人做了，本件不知道」：

| 缺陷                                                                  | 邻居是谁                                               | 表现                                                                           |
| --------------------------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------ |
| `Progress` 把 `value` 解构走之后没传回 Radix `Root`，只用来算位移     | **上游 shadcn**（这个 bug 是照抄进来的）               | 色条画得好好的，但读屏器听到的永远是「正在忙」——`aria-valuenow` 一次都没发出去 |
| `ShellUserMenu` 在 `StatusBadge` 的语气图标之外又显式画了一个 `check` | **`StatusBadge` 自己**（文件头：「不必每处各配一张」） | 「已认证」前面并排两个对勾                                                     |

两条都不报错，都不影响布局，都在**看得见的地方**躺了很久。它们躲过审查的方式也一样：**读代码时那一行看起来是对的**——`value` 明明在用，`check` 明明该画。要看出问题，得同时打开邻居的文件。

> 组件调组件的地方，问一句：**这件事被做过两遍，还是一遍都没做？**
> 单看本文件永远答不上来。答案在 `Root` 的 props 表里，和邻件的文件头里。

`Progress` 那条另有一层：它是**照抄上游抄进来的 bug**。本仓的文件头写着「结构照 shadcn 官方 Progress」——那句话此前读起来是出处说明，现在它同时是一条线索：**上游的缺陷会一并抄进来，而「照抄上游」通常被当作不必再审的理由。**

### 5.1.7 第六批学到的：**编译不过的「红」不算抓到**

变异器最初只认 `Tests N failed`。把 `<TooltipProvider>` 换成 `<>` 而不动闭标签，文件直接编译不过——输出里没有那一行，于是报「全绿，没人管这条」，看起来像测试有洞。

反过来更危险：如果当时按「非零退出码 = 抓到」来判，那条变异会被记成**已验证**，而实际上没有任何一条断言跑过。

同一轮还撞到第三种结局。有一条变异（把可选回调 `onDensityChange?.(value)` 改成硬调用）确实炸了——但炸成的是 vitest 的 **Unhandled Error**，没有任何一条断言接住它，那 50 条用例全绿。

洞在用例这边：它写的是

```ts
await expect(user.click(...)).resolves.toBeUndefined();   // ← 装饰
```

**React 事件处理器抛出去的错不在这条 promise 链上。** `user.click` 照样 resolve，异常只以「未捕获」的形式出现在跑批日志的角落里。要真的断言「点下去不抛」，得去接 window 的 `error` 事件：

```ts
const onError = vi.fn();
window.addEventListener("error", onError);
await user.click(...);
window.removeEventListener("error", onError);
expect(onError).not.toHaveBeenCalled();
```

变异器现在分四种结局：抓到 / 全绿 / **编译不过（不算数）** / **炸在未捕获异常上（同样算洞——缺陷是炸了，但没人接住）**，并支持一条变异由多处改动组成（`edits`），供开闭标签这类成对修改使用。

### 5.1.8 待办：有一整类文件从来没被格式检查过

`format:check` 的 glob 是 `"**/*.{js,ts,tsx,json,md}"` —— **没有 `mjs`**。

而 `scripts/` 几乎全是 `.mjs`：十条守卫、令牌生成器、审计快照、守卫自测，一个都不在检查范围里。这一条是在给 `snapshot.mjs` 补一行时撞见的——那一行 prettier 说不合规，而它是 #34 就写进去的，一路绿到今天。

实测：27 个 `.mjs/.cjs` 里 **16 个有真实的格式差异**（其余 11 个只是本机 CRLF）。

改法是把 `mjs,cjs` 加进 glob 并跑一次 `--write`。**没有随本轮一起改**：16 个文件的重排会把一个测试批次的 PR 冲成一片噪音，读不出真正改了什么。留一个单独的小 PR。

> 一条检查的**覆盖面**和它的**判据**一样重要。判据写错了会报假绿；覆盖面漏了一整类文件，则连绿都不报——它根本没看那边。
